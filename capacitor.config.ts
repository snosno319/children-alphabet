import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.englishadventure.app',
  appName: 'English Adventure',
  webDir: 'dist'
};

export default config;
